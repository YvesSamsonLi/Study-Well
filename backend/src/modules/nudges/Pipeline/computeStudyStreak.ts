export async function computeStudyStreak(userId: string): Promise<number> {
  // TODO: query study sessions from DB
  // Example: check consecutive days with study sessions
  const streak = Math.floor(Math.random() * 5); // stub
  return streak;
}
